package com.microserv.trivia;

import org.junit.*;
import static org.junit.Assert.*;

/**
 * Unit test for simple TriviaApp.
 * mvn test to run test classes
 * This will compile the code and then run the unit tests
 */
public class TriviaAppTest {

    /**
     * Rigorous Test :-)
     * Each test method has @Test before it
     * Each test method should test only 1 thing
     * 
     * Generally bare minimum: have 1 test method for every method in the class/for each change/enhancement
     */
    @Test
    public void testApp()
    {
    	/*
    	 * Usually each test method will eventually have some sort of assert method
    	 * assert says this thing is true and if it isn't the test fails
    	 */
        assertTrue( true );
    }
}